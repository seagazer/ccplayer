/**
 * The type to define the gesture actions include setBrightness, setVolume and seek.
 *
 * Author: Seagazer
 * Date: 2024/10/10
 */
export enum GestureType {
    /**
     * The type to change brightness by gesture.
     */
    BRIGHTNESS,
    /**
     * The type to change seek progress by gesture.
     */
    PROGRESS,
    /**
     * The type to change volume by gesture.
     */
    VOLUME
}